spring:
  application:
    name: springboot4k8s
  datasource:
    url: jdbc:postgresql://host.minikube.internal:5442/spring_poc
    username: marceloserpa
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 9000